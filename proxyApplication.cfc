<cfcomponent extends="Application">
	<!--- 
	WORKAROUND:
	the proxyApplication.cfc provides a workaround for CF7 issue 
	preventing Appliction.cfc from extendig another Application.cfc directly.
	 --->
</cfcomponent>